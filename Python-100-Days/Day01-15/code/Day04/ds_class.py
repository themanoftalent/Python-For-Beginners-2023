from turtle import done


class GuvenlikGirisi:

    def login(self, sifre):
        sifre = (int(input('Enter a password')))
        done != 123
        while done:
            break

    def selam(self, isim, yas, memleket):
        self.isim = isim
        self.yas = yas
        self.memleket = memleket
        print(isim, yas, memleket)



kisi1_sifresi = GuvenlikGirisi()
kisi1_sifresi.login(kisi1_sifresi)


while kisi1_sifresi==123:
    continue

kisi1_sifresi.selam('Ahmet', 24, 'istanbul')
